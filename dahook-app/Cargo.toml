[package]
name = "dahook-app"
version = "0.1.0"
edition = "2021"
description = "dahook: um app so. Terminal (kitty) que vira browser isolado visualmente. X volta ao terminal."

[dependencies]
clap = { version = "4", features = ["derive"] }
anyhow = "1"
