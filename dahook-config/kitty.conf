# dahook — configuração do terminal (backend: kitty).
# Este arquivo é do APP dahook (não mexe no ~/.config/kitty do usuário).
# Referência de todas as opções: kitty/kitty/options/definition.py
# Atalhos: todos os do kitty valem aqui (ver --debug-config para a lista efetiva).

# Identidade (também passada por flags --class/-T no executável ./dahook)
# window_title dahook
